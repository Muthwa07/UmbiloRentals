@using UmbiloRentals.Models

@{
    ViewBag.Title = "Apply for Room";

    var room = ViewBag.Room as Room;
}

<div class="application-page">

    @if (room == null)
    {
        <div class="glass-card application-error-card">

            <div class="glass-alert error">

                <i class="bi bi-x-circle-fill icon-danger"></i>

                <div>
                    <strong>Room not found</strong>

                    <p>
                        We could not find the room you are trying to apply for.
                        Please return to the rooms page and select a valid room.
                    </p>
                </div>

            </div>

            @Html.ActionLink(
                "Back to Rooms",
                "Index",
                "Rooms",
                null,
                new { @class = "btn-back-room" }
            )

        </div>
    }
    else
    {
        <div class="application-grid">

            <!-- LEFT SIDE -->

            <div class="application-preview glass-card">

                @if (!string.IsNullOrEmpty(room.Photo))
                {
                    <img src="@Url.Content("~/Content/RoomImages/" + room.Photo)"
                         class="application-image"
                         alt="Room @room.RoomNumber" />
                }
                else
                {
                    <div class="no-photo-placeholder" style="border-radius:24px; height:320px;"><i class="bi bi-house-door"></i><span>No photo available</span></div>
                }

                <div class="application-info">

                    <span class="room-status available">
                        Available
                    </span>

                    <h2>Room @room.RoomNumber</h2>

                    <div class="rent-chip">
                        R @room.MonthlyRent / month
                    </div>

                    <p>@room.Description</p>

                    <div class="mini-features">

                        <span>
                            <i class="bi bi-shield-check icon-info"></i>
                            Secure
                        </span>

                        <span>
                            <i class="bi bi-wifi icon-info"></i>
                            Wi-Fi
                        </span>

                        <span>
                            <i class="bi bi-car-front-fill icon-info"></i>
                            Parking
                        </span>

                    </div>

                </div>

            </div>

            <!-- RIGHT SIDE -->

            <div class="glass-card application-form-card">

                <div class="account-header">

                    <h1>Complete Your Application</h1>

                    <p>
                        You're applying for
                        <strong>Room @room.RoomNumber</strong>.
                    </p>

                </div>

                @if (ViewBag.ErrorMessage != null)
                {
                    <div class="glass-alert error">

                        <i class="bi bi-x-circle-fill icon-danger"></i>

                        <div>

                            <strong>Application couldn't be submitted</strong>

                            <p>@ViewBag.ErrorMessage</p>

                        </div>

                    </div>
                }

                @using (Html.BeginForm(
                    "Create",
                    "Applications",
                    FormMethod.Post,
                    new { enctype = "multipart/form-data" }))
                {
                    @Html.AntiForgeryToken()

                    <input type="hidden"
                           name="roomID"
                           value="@room.RoomID" />

                    <div class="upload-card">

                        <i class="bi bi-upload icon-info icon-2xl"></i>

                        <h3>Supporting Document</h3>

                        <p>
                            Upload <strong>one combined document</strong>
                            containing your <strong>ID</strong> and
                            <strong>Proof of Income</strong>.
                        </p>

                        <input type="file"
                               name="document"
                               accept=".pdf,.doc,.docx,.jpg,.jpeg,.png"
                               required />

                        <small>
                            PDF, Word, JPG or PNG • Maximum 5 MB
                        </small>

                    </div>

                    <div class="application-note">

                        <i class="bi bi-info-circle-fill icon-info"></i>

                        Your application will be reviewed by our
                        accommodation team before approval.

                    </div>

                    <div class="form-group consent-group">
                        <label class="consent-checkbox">
                            <input type="checkbox"
                                   name="consentGiven"
                                   value="true"
                                   required
                                   aria-required="true" />
                            <span>
                                I consent to Umbilo Rentals processing the
                                document I've uploaded for the purpose of
                                reviewing this application, as described in the
                                <a href="@Url.Action("Privacy", "Legal")" target="_blank" rel="noopener noreferrer">Privacy Policy</a>.
                            </span>
                        </label>
                    </div>

                    <button type="submit" class="btn-auth">
                        Submit Application <i class="bi bi-arrow-right"></i>
                    </button>
                }

                @Html.ActionLink(
                    "Back to Room",
                    "Details",
                    "Rooms",
                    new { id = room.RoomID },
                    new { @class = "btn-back-room" }
                )

            </div>

        </div>
    }

</div>